<div id="<%= dom_id quest %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
    <h3 class="font-bold text-lg <%= 'line-through text-gray-500' if quest.status? %>">
      <%= quest.title %>
    </h3>
  </div>

</div>
