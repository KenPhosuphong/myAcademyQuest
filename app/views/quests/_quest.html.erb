<div id="<%= dom_id quest %>" class="w-full sm:w-auto my-5">
  <div>
    <h3 class="font-bold text-lg truncate min-w-0 max-w-[80%] <%= 'line-through text-gray-500' if quest.status? %>" title="<%= quest.title %>">
      <%= quest.title %>
    </h3>
  </div>
</div>